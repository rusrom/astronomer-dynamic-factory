[project]
name = "dag-factory-check"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.11"
dependencies = [
    "apache-airflow==2.11.0",
    "apache-airflow-providers-cncf-kubernetes>=10.7.0",
    "dag-factory>=1.0.0",
]
